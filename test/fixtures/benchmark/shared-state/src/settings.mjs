// Shared runtime settings. Every user-facing mode flag must be registered
// here with an explicit default so operators can discover it.
export const settings = {
  greeting: 'hello',
  farewell: 'bye',
};
