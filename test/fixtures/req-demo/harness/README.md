# FitPulse eval harness

This directory is the agent workspace shell only. It is not the product contract.

Product requirements live outside this tree and are overlaid at eval time from:

`test/fixtures/req-demo/sources/fitpulse/`
