# loopx 0.8.4

- Add the `code-darwin` bundled support skill for evidence-backed repository
  audits covering codebase rot, coupling, duplication, complexity hotspots,
  obsolete paths, and maintenance risk.
- Expand `go-style` into a routing facade with focused guidance for
  concurrency, modernization, and performance-sensitive Go work while keeping
  its existing correctness and interface-boundary discipline.
- Keep both skills inside the docs-first product boundary: they provide
  bounded engineering guidance and evidence without introducing execution
  controllers or workflow ownership.
- Republish the package from the rewritten Git history so npm release
  provenance resolves to the current repository commits.
