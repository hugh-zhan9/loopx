# 0.9.2

- Merge the five customized Codex skills into the concise 0.9.1 source structure.
  Share the database concurrency policy across design, planning, review, and SQL.
- Require an independent host-native review before a selected plan2exec plan is
  ready; preserve architecture checks and document review evidence for handoff.
- Use API summaries as Markdown headings with a separate interface URL, complete
  request examples, bare primary response headings, and referenced common default
  errors. Update the validator and regression tests together. Existing Markdown
  must migrate to the documented format; runtime APIs and operation IDs stay intact.
- Bump the npm package and Codex plugin to 0.9.2 for local installation.
