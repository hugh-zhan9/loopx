# loopx 0.2.1

## Summary

`0.2.1` adds finish-time learning extraction to the v1 skill suite.

## Changed

- Updated `finish` to extract local agent memory before presenting merge, PR, keep, or discard options.
- Documented `.loopx/memory/MEMORY.md`, `.loopx/memory/index.jsonl`, active memory entries, and retired memory cards.
- Added repo-tracked spec candidate guidance for `docs/loopx/specs/<domain>.md` with `docs/loopx/specs/inbox.md` as the fallback.
- Updated public docs and governance checks for finish memory extraction and spec candidates.
