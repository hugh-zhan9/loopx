# loopx 0.9.1

- Simplify skill entrypoints and load detailed templates only when needed.
  Preserve useful execution guidance while removing repeated instructions,
  unnecessary confirmation, and mandatory full reports for focused requests.
- Preserve proposal status, factual uncertainty, ownership, and explicit
  boundaries during document rewrites. Make prompt and requirement scoring
  conditional, and retain active plans during documentation cleanup.
- Align design, overview, refactor, plan, and issue-repair handoffs. Keep design
  anchors and architecture conformance traceable through implementation; require
  content checkpoints rather than filenames alone when resuming execution.
- Fix worktree examples for the selected ignore directory, zsh variables, and
  paths containing spaces. Reuse verification evidence while the tested state
  remains unchanged, without implying broader coverage than was checked.
- Track shared skill contracts individually during installation so unchanged
  managed files can upgrade while user-modified and foreign files are preserved.
  Recognize exact pristine 0.8.9/0.9.0 completion/evidence contracts when older
  installations lack per-file baselines, without overwriting customized content.
  Reject writes through shared-directory symlinks to protect their linked source.
