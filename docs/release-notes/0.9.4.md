# 0.9.4

- Require API response fields to have an evidenced caller use. Select fields for
  each scenario instead of exposing database entities, full snapshots, or
  diagnostic metadata by default.
- Return structured data as defined objects or arrays when callers need to
  inspect it; JSON text storage does not justify JSON-encoded response strings.
  Preserve existing compatibility contracts and justify any raw-text output.
- Apply the response rules in the detailed design template. Upgrade api-designer
  to 0.3.10 and spec to 0.4.8. The local spec-v2 trial adopts the same rules at
  0.2.1 and remains outside the bundled installation.
- Bump the npm package and Codex plugin to 0.9.4 for local installation.
