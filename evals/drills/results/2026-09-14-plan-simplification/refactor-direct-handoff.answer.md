直接依据已批准的 RFC，在当前会话内实施。先检查工作区和相关代码，核对既定测试基线，然后按方案步骤将同一模块内重复的私有格式化逻辑提取为内部函数；每步使用既定验证确认外部输出和异常保持不变，失败时按方案排查或回退。最终核对写入范围、差异和验证结果。

RFC 可以直接指导已获授权的实施，无须转换为 `loopx-plan/v1`，也无须等待独立计划评审。这里没有额外持久协调需求，用户也未要求另一份计划或委派；`loopx-plan/v1` 是选择通过 `$exec` 委派时的要求。实施授权不包含提交、推送或合并。
