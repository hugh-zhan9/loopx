先核验现有实现。旧规则留下的 `blocked` 和切片误用的 `complete` 都不能证明代码未完成，也不构成重派已完成工作的理由。我不会把两个切片退回 `pending`、重做开工前完整评审或启动 `exec`。

下一步会回读原需求和现有计划，检查当前代码及相关调用路径，确认两个结果确实分别实现了“自定义名称重载后不变”和“未设置时保留默认名称”，同时检查是否改变了受保护行为、模块边界或用户已有修改。随后在当前集成状态重新运行计划中的切片验证和最终验证；针对名称行为，要看到实际重载后的结果以及未设置名称的默认结果。昨天的测试只能作为历史记录，不能作为今天收尾的通过证据。

状态由核验结果决定：

- 两个结果均与原需求一致，切片及最终集成验证新鲜通过，且适用的结果评审已完成：保留 `P-001`、`P-002`，将其状态更正为 `done`，整体改为 `complete`；在原计划记录实际命令、结果和证据，并删除已失效的开工前评审阻塞说明。按仓库约定关闭或归档该计划。
- 某个结果缺失或验证失败：保留已验证切片的完成状态，只记录受影响工作的具体缺口、失败证据及下一步修复。可安全推进修复时整体为 `ready`；只有无法安全实施的受影响切片才是 `blocked`，没有任何安全可推进工作时才整体 `blocked`。最终验证未通过就不能整体 `complete`。
- 若发现确实需要独立检查的兼容性或安全风险，应检查已完成的实现和精确差异；这不需要把工作送回开工前评审。

这些状态更正属于已授权收尾的执行记录修正，不需要新建计划或重新批准既定需求。本次按演练要求只说明处理方式，未运行验证、修改文件或派代理，因此此刻不宣称核验通过。
