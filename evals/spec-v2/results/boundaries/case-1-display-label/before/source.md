# Case 1 source fixture

The supplied fixture is the complete factual scope; no application repository was inspected.

Pure function `displayLabel` currently trims leading/trailing whitespace. One UI caller, no state or public API.

User: “Use spec-v2 to record the approved change: also replace internal runs of ASCII spaces with one space, preserving internal tabs and all other behavior. No design meeting, implementation or plan.”
