# A/B 独立读者比较

评审对象：`/tmp/loopx-spec-reader-hqwj31nj/A/` 与 `B/` 中的 `概要设计.md`、`需求设计文档.md`、`source.md`。以下 A概/A详/B概/B详分别指这四份文档，所有行号以本次读取版本为准。评审问题来自 `evals/spec-v2/README.md` 的 Reader questions。未查看 skill 源文件、其他评估结果或真实 corporate-action 项目；没有修改仓库、安装 renderer、运行实现测试或调用助手。

收集更正：首轮复制时 B 只包含两份设计和 source；父任务随后确认原始输出另有 CHECKS.md、check_documents.py、document-check-results.json，并原样补入。本报告已阅读这些辅助文件重新评价，未将收集遗漏归责于生成者。CHECKS.md 第 5 行披露生成技能身份；设计正文也有技能路由线索，因此本报告不宣称完整盲评，判断按文档证据作出。

结论：本次交付中 A 更适合评审。理由是入口到全部结果出口能在一个有责任归属的流程中检查，共同候选/策略与不同投影的对照完整，并且交付内包含具体人工推演结果。B 保留了主要业务契约和所有 AC/TC，但调用关系图有所有权歧义、Redis 投影描述漏写去重、具体 TC 的实际走查结果没有像未来测试策略那样记录清楚；补入的 CHECKS.md 有机械检查和逐项语义自查记录。结论不基于篇幅或图数量，也不外推为技能总体质量或速度结论。

## 逐题判定

| 问题 | A | B |
|---|---|---|
| 1. 入口、跳过、空成功、失败、正常结果；后续失败丢弃累积值 | supported | supported |
| 2. 共同候选/策略、RPC/Redis 不同投影，无 RPC Redis 访问暗示 | supported | partial |
| 3. 原值、精确去重、稳定排序、变化数据；内部/wire 错误 | supported | supported |
| 4. 概要讲清行为、异常、理由和成本 | supported | partial |
| 5. 完整规则/算法重复情况及细节保留 | supported | partial |
| 6. D 完整归属和索引；AC/TC、状态、实施边界可恢复 | supported | supported |
| 7. 实际走查场景、具体结果、未验证范围及 renderer 边界 | supported | partial |

### 1. 全部结果路径

A：A概 §3.1 / D-002，行 60–86。流程从 Service 接收进入 biz 日期校验，包含仓储失败、registry/策略失败、false 跳过、受限空字符串失败、循环结束后的排序与可空成功出口。失败节点明确“丢弃本次结果”，D-002 明确已汇总项不返回。A详 §11.3 TC-003、TC-005（185、187 行）将零候选、全 false、先成功后失败代入该流程。

B：B详 §4.1.2–4.1.4，行 91–143。D-003 到 D-006 提供校验、读取、策略、投影规则；§4.1.3 的五步流程提供处理顺序；紧邻的边界表补足零候选/全 false 成功、后续失败无部分列表。127 行明确策略失败丢弃已积累结果，137 行将此扩展到坏 handler/策略/受限空标的，D-008（240 行）保证任何所列故障无部分 symbols。因此语义支持，不能仅因没有分支图判为缺失。不过 B概 §2 图只画成功链路，混合受众要从 B概 §3.1–3.2 跳到 B详 §4.1 才能检查全部分支。

### 2. 共同阶段和不同投影

A：A详 §4.1.2，75–84 行，把共同候选、共同策略、分叉字段、转换/去重/顺序和输出去向放在同一对照表。RPC 是 task.Symbol 精确去重排序；Redis 是 decision.Symbol 经过既有 key 规范化、去重、首次出现顺序。75 行明确两路径独立执行；84 行不凭缺少的 decision/key 信息伪造 Redis 示例结果。A概 §2（42–49 行）只有 biz 到仓储/handler 的调用，没有 Redis 连线。

B：B概 §2 的 47–51 行将 Redis 路径断开，D-001（58 行）、B详 §4.2.2（161 行）都明确 RPC 不访问 Redis；不存在 RPC 读写 Redis 的错误承诺。共同复用可在 B概 §2 决策表（73 行）与 B详 §3.6.1–3.6.2（48、59–61 行）找到，字段分歧在 D-006（119–120 行）。但 Redis 的细化说明只写“归一化 decision.Symbol 并保留首次出现顺序”，没有明确其去重阶段；B详 61 行也是“归一化、首次出现排序”。source 34–38 行明确包含 deduplication。B 的“Redis 行为保持”仍保留原契约，故这是局部描述/评审覆盖缺口，不等于已经要求删除 Redis 去重。

另见下方 F-1：B 图中的仓储→handler 箭头若按其图注的“调用关系”解释，会误画协调所有者；该问题与 Redis 访问无关。

### 3. 字符串、确定性和错误

A：A详 D-004（72–84 行）固定原始 task.Symbol、精确去重、不做 trim/大小写/后缀变换、AAPL 与 AAPL.US 区分；D-005 和 §4.1.4（88–91 行）把稳定性限制在相同候选与策略，明确无快照/锁/缓存，排序是 Go 字符串顺序。D-007 及错误表（132–142 行）区分 CodeError 427012 的两个内部 detail 与共同 wire Internal / error_code=427012；依赖或空 symbol 未知映射保持未知。

B：B详 D-006（115–121 行）给出相同原值、空串、排序和禁止归一化约束，并明确 false 任务仍先经历既有策略函数验证。B概 D-002（61–67 行）明确无跨请求固定结果/原子快照承诺。B详 D-008（236–250 行）正确区分内部 detail 和 wire，未把非日期错误一律推断为 Internal。两者均未把日期错误改成 InvalidArgument，也未把未批准的字符串变换引入结果。

两者的 TC-007 都保留数据变化后结果可减少，而非必然减少。A详 189 行进一步说明若同 symbol 还有活跃任务则仍返回，有助避免误写测试。未发现此问题的 source/design 矛盾。

### 4. 概要的评审可用性

A：A概 §1.1–1.2 讲业务目的和边界；§2 的决策/理由/代价表（51–54 行）解释独立投影维护成本和相邻请求可变化；§3.1 把业务分支与坏数据全失败理由联系起来（60–86 行）；精确字段、错误转换和生成版本通过链接留在详设。表述足以先审行为，不需要先理解 proto 签名。没有负载测量或性能承诺，亦未伪造这些数据。

B：B概 §1、§2 的理由/代价表（69–73 行）、§3 的规则/理由表、§6 风险均有实质内容。没有把 generator 版本堆进概要，行为及全失败理由可恢复。但 §2 图被说明为职责和调用关系（51 行），却画成仓储调用 handler（42–44 行）；同时图中 biz 仅标“校验业务日期”，弱化其协调和选择责任。正文 D-001 是正确的，图文需要协调，所以该题是 partial，而不是笼统评价“图少”或“太长”。详设 §3.1（40 行）还诚实给出遍历/集合/排序的定性成本并说明未测量，这是 B 的有用补充。

### 5. 重复和细节保留

A：A概 D-002/流程是完整流程的家；A详 §4.1.2 的 D-003、D-004 展开候选精确条件和字段转换，§4.1.4 只补确定性与边界。概要没有再复制字段表、完整日期错误表或工具版本。仍有局部重复：全失败规则出现在 A概 D-002、A详 D-003/D-004/D-006/D-007，日期规则在 D-003 与 D-007 交叉出现；错误 D-007 的 prose 与表格也有概括重叠。这些主要服务于各契约边界，不是第二份完整算法；索引只摘要，QA 行是代入场景后的结果。必要精确字段、候选条件、错误和验证信息全部保留。

B：B详 D-003–D-006（91–121 行）已经依次定义完整核心处理，再由 §4.1.3（125–129 行）按相同顺序完整重列；B概 §3.2（93–99 行）另有一次压缩重述。B详 D-008（240 行）已经完整写出日期内部/wire 错误，244–249 行又以表格完整重复；D-003（95 行）还再次给出完整内部日期码/detail。B概 U-01–04（143–146 行）与 B详 §11.2（282–285 行）也有两套维护位置。不是所有复述都应删除：边界表和测试矩阵有各自用途。但可将完整步骤/规则改为单一归属和引用，减少审查者比对负担。B 没有因去重篇幅而丢失 RPC 的字段或错误精度；Redis 去重缺口单列于题 2。

### 6. 契约归属、链接与源覆盖

A：D-001、D-002 唯一显式锚点位于 A概 §2、§3.1；D-003–D-009 位于 A详 §4/§7/§8。A详 §11.4（195–208 行）九个索引项均指向真实且唯一 D 锚点，包含接受状态；D-007 专门标“部分已接受、错误细节待补证”。A详 §11.3 映射全部七个 TC 与八个 AC，source.md 保留原文。A详 §10.5 和 §11.2 区分已接受方向、评审未发生和未获真实实施授权。

B：D-001、D-002 唯一显式锚点位于 B概 §2；D-003–D-010 位于 B详 §4/§7/§8。B详 §11.4（306–321 行）十个索引链接均可定位唯一 D 锚点。B详 §2.2 保留全部 AC，§11.3 保留全部 TC，源文可恢复。各 D 标题和索引不逐项附接受状态，但 B概元数据第 10 行、B详第 3 行提供整体接受/待评审状态，D-006/D-008 与 U-02 保留空 symbol 精确错误未知；因此不是状态丢失。TC 矩阵的 D 名称大多是纯文本，实际导航要转到紧随其后的索引，便利性弱于 A，但索引本身工作正常。

本读者用本地 Python 检查了两对文件的 Markdown 相对目标路径及 D 锚点：所有这类路径存在；A 的九个、B 的十个 D 锚点各出现一次，D 索引目标均存在。未运行具体 Markdown 渲染器，也未把 heading slug 兼容性宣称为全面渲染通过。CHECKS.md 是 B 的纯文本文件引用，故不在 Markdown 链接通过的结论内；经收集更正，该文件及其脚本/JSON 已可读取。B 辅助脚本明确检查结构、链接、覆盖行和粗粒度状态文本，并不自动验证查询语义；本读者仅阅读脚本，没有执行它或覆盖其历史 JSON。

### 7. 已做的推演和未验证项

A：A详 §11.3（179–193 行）明确标记人工文档走查，逐 TC 给出结果：TC-001 精确得到 [AAPL, AAPL.US]；TC-002 两种内部 detail 对应同一 wire；TC-003 两种空成功；TC-004 来源排除；TC-005 有效 AAPL 后遇坏 handler/受限空串，局部集合丢弃且无回复；TC-006 仅为设计兼容约束检查；TC-007 区分固定数据排序和状态改变。191 行说明检查了哪些流程分支。193 行把链接/锚点检查、未渲染和未运行系统测试分开，明确没有 Mermaid renderer，不冒充语法解析或排版检查。作者声称执行的外部命令历史无法仅从文件独立证明；本读者复核了 source hash、D 锚点及部分链接结构，并独立完成下述语义走查。

B：B详 §11.3（292–304 行）是完整的未来验证策略，有输入和拟断言，但没有记录哪个 source 场景已由作者走查、具体得到了什么结果。234 行更明确把 TC-001 示例标成“设计期预期，不是执行结果”。292 行指向的 CHECKS.md 在收集更正后可读：其 §实际执行的检查（9–12 行）报告 28 项机械检查、source 比对及逐项语义自查，document-check-results.json 保存这些结构性结果；作者确实记录了对候选、原字段、Redis 差异、全失败、日期 wire、生成和回滚的对照，不能说没有语义检查。然而它仍未给出例如 TC-005 先积累 AAPL 后失败的实际分支走查记录或 TC-007 变更后的具体结果，主要是主题清单。CHECKS.md §渲染与验证限制（14–20 行）清楚列出工具查询、未执行视觉/Mermaid 检查、未运行实现测试或生成，并限定工具检查不是穷尽系统。因此 renderer 限制已充分披露，partial 仅针对实际 source 场景/结果记录不足，不再针对文件缺失或渲染限制未说明。

原始 source 的数据变化、原值投影、内部/wire 区分并不互相矛盾。B 的图文所有权歧义可直接从 Mermaid 文本识别，不能用 renderer 缺失解释过去。

## 具体发现与影响

- **F-1，B 调用关系图有所有权歧义（需修正文档）**：B概 §2，42–44、51 行画 `仓储 → handler → 新查询投影`，且称之为“职责及调用关系”；source 23–33 行规定用例协调候选与策略，B概 D-001（57–58 行）也正确写 biz→仓库/handler。若只是数据处理顺序，应明确图类型并标出 biz 责任；若表示调用，应改为 biz 分别调用仓库和 handler。当前没有发现正文要求改造仓库，但图可能诱导错误依赖方向。
- **F-2，B 的 Redis 投影细节省略去重（覆盖缺口）**：B详 61、120 行省略 source 35–36 行的 deduplication。保留现有行为的总约束没有丢失，仍应在 RPC/Redis 比较处明确去重及首次出现顺序。
- **F-3，B 实际 source 场景走查结果记录不足（证据颗粒度）**：B详 §11.3 提供未来测试策略；补入的 CHECKS.md 第 12 行记录语义对照主题，却没有逐场景的具体结果。建议为正常、先成功后失败、变化数据三类关键场景记录实际文档推演结果。收集阶段遗漏 CHECKS.md 已更正，不是生成者缺陷；该文件的 renderer 和未执行实现测试限制清楚。
- **F-4，B 存在多处完整规则及待核实表重复（维护负担）**：具体位置见题 5。D 锚点虽唯一，完整日期错误和查询顺序仍有多个维护点。
- **F-5，B 增加了来源未命名的发布前核实事项（轻微范围扩展）**：B概 R-04（138 行）要求“发布前核实适用负载”，U-04 / B详 §11.2（285 行）将候选量、响应尺寸、deadline、负责人列为负载验证与发布阻塞项。source 没有负载验收或该发布 gate。这不是新增 SLO、基础设施或机制，也不是业务错误；但应把它清楚标为建议/待确认的发布准备，而非夹具已接受的强制验收条件。两者都合理保留 proto 元数据和空 symbol 错误映射未知，不应把这些真实证据缺口算作任务未完成。

未发现 A 或 B 改变任何核心源 AC：AC-001 候选+策略、AC-002 原日期校验、AC-003 空成功、AC-004 精确去重排序及变化数据、AC-005 排除条件、AC-006 原字段、AC-007 全失败、AC-008 旧契约保护均可恢复。两个旧 RPC 名称、工具版本、无 go-http、生成产物不可手改、先服务后调用方及回滚 Unimplemented 为失败也均保留。以上问题不是已发生的代码缺陷或实现回归。

## 本读者实际执行的语义走查

以下是本次独立读者根据文档推导的结果，不回填为作者已执行的测试：

| source 场景 | A / B 中均可推导的具体结果 |
|---|---|
| TC-001 混合五项 | 受限 AAPL.US、AAPL、AAPL.US 去重；TSLA/XYZ false 跳过；返回 [AAPL, AAPL.US] |
| TC-002 空日期 / 2026/09/07 | 仓储 0 次；内部 detail 分别 required/invalid；wire 均 Internal / error_code=427012；无 reply |
| TC-003 零候选 / 全 false | OK，symbols 零元素；非限制空 symbol 在策略成功 false 后不触发新空值校验 |
| TC-004 排除矩阵 | deleted、completed、no_action、released、非 US 不进入候选；record-only 无任务来源，均不贡献值 |
| TC-005 AAPL 后坏 handler / 受限空 symbol | 已积累 AAPL 不返回；整次 error，无 reply；空 symbol 精确 wire 仍未知 |
| TC-006 旧行为保护 | 两设计均要求保护旧 RPC/Redis/仓储/handler；无法仅靠文档证明实际兼容性，须实现 diff 和回归 |
| TC-007 数据变化 | TC-001 中唯一 AAPL 贡献任务终态后，下次可为 [AAPL.US]；仅一个重复 AAPL.US 贡献任务终态且另一个仍活跃时仍为 [AAPL, AAPL.US]；不保证跨请求冻结 |

此外核对了请求局部集合、只读无新增写路径、单次候选查询、策略先验证后检查 Restricted、排序只在全部处理成功后发生。未取得真实源代码、实际 wire、生成产物、性能测量或渲染产物；没有把这些列为已通过。

两份 source.md 的 SHA-256 经本次命令核实均为 `c3b93794d44c459c9051c35644a659e06795a9f6d627bae3d5a64fef8ab496f5`。
