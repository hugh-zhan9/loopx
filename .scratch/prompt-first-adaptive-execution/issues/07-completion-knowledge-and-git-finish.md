# 07 — Distill completion knowledge and narrow finish

**What to build:** Add one quiet completion check for direct, sequential, and concurrent work that keeps applicable specs consistent and preserves only genuinely reusable knowledge. Make finish appear only when the user explicitly wants Git disposition.

**Blocked by:** 03 — Deliver lean plans and one serial exec entry.

**Status:** ready-for-agent

- [ ] Every completion path confirms fresh verification and checks whether an applicable spec changed.
- [ ] Behavior changes governed by an existing spec update that spec as part of the implementation.
- [ ] A new durable rule is written only when backed by an explicit user decision, approved requirement, or existing spec authority.
- [ ] Local memory is written automatically only for an encountered, evidence-backed, non-obvious, reusable project pitfall.
- [ ] Ordinary changes produce no generic memory, shared-memory, or spec review reminders.
- [ ] Shared memory and newly tracked knowledge require explicit acceptance and never contain secrets, raw conversation, or workflow state.
- [ ] Finish handles only valid commit, branch, merge, pull request, keep, cleanup, or discard choices and has no final-review or extraction precondition.
